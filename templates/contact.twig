<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Luxury Private Jets - Contact Us</title>
    <link rel="stylesheet" href="contact.css">
</head>
<body>

<header>
    <h1>Luxury Private Jets</h1>
    <nav>
        <a href="index.php">Home</a>
        <a href="jets.php">Browse Jets</a>
        <a href="contact.php">Contact Us</a>

        {% if is_logged_in %}
            <a href="user_dashboard.php">My Account</a>
            <a href="user_logout.php">Logout</a>
        {% else %}
            <a href="user_login.php">Login</a>
            <a href="user_register.php">Register</a>
        {% endif %}
    </nav>
</header>

<main>
    <section id="contact-form">
        <h2>Get in Touch</h2>
        <p>If you have any questions about our jets or need further assistance, please reach out!</p>
        
        <!-- Success message after form submission --> 
        {% if successMessage %}
            <div class="message">
                <p>{{ successMessage }}</p>
            </div>
        {% endif %}
        
        <form action="contact.php" method="POST">
            <label for="name">Name:</label>
            <input type="text" name="name" id="name" required>

            <label for="email">Email:</label>
            <input type="email" name="email" id="email" required>

            <label for="message">Message:</label>
            <textarea name="message" id="message" required></textarea>

            <!-- Optional hidden input for jet_id if user is contacting about a specific jet -->
            <input type="hidden" name="jet_id" value="{{ app.request.get('id') }}">

            <button type="submit" class="btn">Submit</button>
        </form>
    </section>
</main>

<footer>
    <p>&copy; {{ "now"|date("Y") }} Luxury Private Jets. All rights reserved.</p>
</footer>

</body>
</html>